name: CI

on:
  push:
    branches: [ main, "work/**" ]
  pull_request:

jobs:
  dotnet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Build CLI
        run: dotnet build src/cli/MiPush.Cli/MiPush.Cli.csproj --configuration Release

  android-stub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.0'
      - name: Build Android stub flavor
        working-directory: src/android
        run: gradle --no-daemon :app:assembleStubDebug
